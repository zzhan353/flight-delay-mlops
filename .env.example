# NOAA API is keyless for the access/services endpoint used here.
# Azure settings are only needed for `--backend azureml` and deployment.
AZURE_SUBSCRIPTION_ID=
AZURE_RESOURCE_GROUP=rg-flight-delay
AZURE_ML_WORKSPACE=flight-delay-ws
MODEL_NAME=flight-delay-classifier
